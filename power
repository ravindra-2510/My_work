/* 2^5=32...
*/
#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {

    /* Enter your code here. Read input from STDIN. Print output to STDOUT */   
     int num,power,i,temp;
     printf("enter the value :\n");
    scanf("%d",&num);
    printf("enter the power value: \n");
    scanf("%d",&power);
    temp=pow(num,power);
    printf("%d",temp);
    
}
